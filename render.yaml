services:
  - type: web
    name: centinela-runtime-api
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn apps.centinela_runtime.main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.10.0
