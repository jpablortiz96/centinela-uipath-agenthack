# Chaos Console

This is a local demo UI designed for judges. It provides a visual dashboard to demonstrate the end-to-end flow of the CENTINELA fraud dispute case management system without connecting directly to UiPath.

## Running the Service

```bash
python -m uvicorn apps.chaos_console.main:app --reload --port 8050
```
